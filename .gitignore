node_modules
dist
.DS_Store
.env
.env.local
server/public
vite.config.ts.*
*.tar.gz

# Windows Zone.Identifier files (WSL artifact)
*:Zone.Identifier
*.Zone.Identifierhpc/embeddings_output.json
hpc/episodes_export.json
hpc/models/
hpc/offline_pkgs/

# Upload directories — keep .gitkeep sentinel files only
uploads/media/*
!uploads/media/.gitkeep
uploads/videos/*
!uploads/videos/.gitkeep

# Server assets — cookies only, keep fonts/ tracked
server/assets/youtube-cookies.txt
